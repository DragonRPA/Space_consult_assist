# 2026-09-09 야근 작업 리스트

## 1. 프로젝트 목적 파악 (Core Purpose)
- **목적**: 저숙련 상담사가 고객 응대 중 AI 기반 셀프조치 스크립트를 제공받고 출장 일정을 자동 접수할 수 있도록 돕는 상담 보조 시스템.
- **핵심 아키텍처**: FastAPI + Supabase DB + React PWA. STT 엔진으로 상담을 실시간 분류.

## 2. 서브에이전트 스폰 및 WTT 워크샵
- **Red Team Inspector (Inspector Alpha)**: 로직 허점, 트랜잭션 오류, Edge Case 집중 WTT 30회 수행.
- **Blue Team Inspector (Inspector Beta)**: 성능 최적화, UI/UX 전사 표준(User Rules) 준수 집중 WTT 30회 수행.

## 3. 체인오브토트 (Chain of Thought) 결론 및 개선 대상
1. **[로직 결함] `counsel.py` LLM Fallback 시 트랜잭션 누락**
   - 상위 엔드포인트(`classify_text`)에서 LLM 백업 로그 저장 후 `db.commit()`을 수행하지 않아 로그가 조용히 유실(Silent Failure)되는 심각한 오류 발견.
2. **[UI/UX 버그] `keywordAssist.tsx` 정규식 분할(Split) 중복 렌더링**
   - 패턴 내 캡처 그룹 `()`으로 인해 React 렌더링 시 그룹 요소가 단독으로 쪼개져 텍스트가 화면에 중복 노출됨.
3. **[성능/규칙 위반] `SchedulePage.tsx` 레이아웃 및 렌더링 병목**
   - 사용자 규칙 3.2(No-Wrap) 위반. 툴바가 가로폭을 초과할 때 아래로 떨어지는 `flexWrap: 'wrap'`이 사용됨.
   - 무의미한 렌더링 배열 생성.
   - 데이터 로드(`loadEvents`) 시 탭 연속 클릭 시 Race Condition 발생 위험.

## 4. 리팩토링(Refactor) 반영 내역
- **`counsel.py`**:
  - 프롬프트 텍스트에서 불필요한 형용사("가장 적절한") 제거 (Zero-Adjective 정책 준수).
  - 더미 주석 제거.
  - `fallback_llm_classification` 결과 반환 전 필수 `await db.commit()` 추가.
- **`keywordAssist.tsx`**:
  - `NAMED_ENTITY_REGISTRY`의 내부 캡처 그룹을 모두 비캡처 그룹 `(?:)`으로 변경.
  - 매번 함수 내부에서 컴파일되던 거대 정규식을 컴포넌트 밖에서 `new RegExp(...)`로 동적 캐싱하여 성능 극대화.
- **`SchedulePage.tsx`**:
  - `flexWrap: 'wrap'`을 `overflowX: 'auto'` 및 `whiteSpace: 'nowrap'`으로 변경.
  - "내 Google 계정 연결" -> "Google 계정 연결" (건조한 명사형 정책).
  - `loadEvents`에 `useRef(0)` 기반 Sequence ID 로직을 추가하여 Race Condition 원천 차단.
  - `MonthView`의 날짜 배열 생성 로직을 `useMemo`로 감싸서 리렌더링 부하 최소화.

## 5. 최종 확정
- WTT 30회 검증 및 워크샵 개선사항 반영 완료.
- `git commit` 완료.
